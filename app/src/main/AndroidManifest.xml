<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.margarita.project6">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityIncome" />
        <activity
            android:name=".Activity_outcome"
            android:label="@string/title_activity_costs"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Activity_categories_of_income"
            android:label="@string/title_activity_categories_of_income"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Activity_categories_of_outcome"
            android:label="@string/title_activity_categories_of_outcome"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>